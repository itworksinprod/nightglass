{
  "name": "nightglass",
  "version": "0.1.1",
  "private": true,
  "type": "module",
  "description": "A privacy-first, cross-browser dynamic dark-mode extension for desktop and iPhone Safari.",
  "scripts": {
    "build": "node scripts/build.mjs",
    "check": "node scripts/check.mjs",
    "test": "node --test tests/*.test.mjs",
    "package": "node scripts/package.mjs",
    "verify": "npm run build && npm run check && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
